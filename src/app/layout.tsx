import './globals.css';
import type { Metadata } from 'next';
import Header from '@/components/Header';
import Footer from '@/components/Footer';

export const metadata: Metadata = {
  title: 'GDG Santa Cruz - Comunidad de desarrolladores Google en Bolivia',
  description: 'Comunidad creada para compartir conocimientos y desarrollar tecnologías Google en aplicaciones móviles, empresariales, sociales, web o tecnología emergente',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="es">
      <body className="min-h-screen flex flex-col">
        <Header />
        <main className="flex-grow">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
} 